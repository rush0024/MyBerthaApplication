<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myberthaapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PasswordChecker"></activity>
        <activity android:name=".Password" />
        <activity android:name=".DataListItemAdapter" />
        <activity android:name=".DataListView" />
        <activity android:name=".HttpHelper" />
        <activity android:name=".ReadHttpTask" />
        <activity android:name=".DataWristBand" />
        <activity android:name=".DataActivity" />
        <activity android:name=".Data" />
        <activity android:name=".DataListItem" />
        <activity android:name=".AddData" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>